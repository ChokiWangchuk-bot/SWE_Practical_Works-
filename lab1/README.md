# Array Manipulation & Function Implementation in Python

## 📌 Overview
This project demonstrates basic array (list) manipulation in Python using stack operations.  
The main task was to reverse a list using `.append()` and `.pop()` methods and then implement the same logic inside a function.
